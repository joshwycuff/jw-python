[tool.poetry]
name = "jw-logging"
version = "0.0.0"
description = "JW logging utility library"
authors = ["Josh Wycuff <Joshua.Wycuff@turner.com>"]
license = "MIT"
readme = "README.md"
packages = [{include = "jw_logging"}]

[tool.poetry.dependencies]
python = ">=3.8,<4"

[tool.poetry.group.dev.dependencies]
pytest = "^7.1.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
